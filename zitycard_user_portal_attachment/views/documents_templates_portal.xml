<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_my_documents" name="My Projects">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
           <div class="o_project_portal_attachments">
                   <h2 class="mb-3">Documentos</h2>
                   <hr class="mt-1 mb-2"/>
                   <div class="row">
                        <t t-if="not attachment_ids">
                            <div class="alert alert-warning" role="alert">
                                No tienes documentos.
                            </div>
                        </t>
                       <t t-else="" t-foreach="attachment_ids" t-as="attachment">
                        <div class="col-md-6">
                            <ul class="list-unstyled">
                                <li class="oe_attachments">
                                    <a t-attf-href="/web/content/#{attachment.id}?download=true&amp;access_token=#{attachment.access_token}"
                                    target="_blank" data-no-post-process="" class="d-flex align-items-center rounded bg-light p-2">
                                        <div class='oe_attachment_embedded o_image o_image_small me-2 me-lg-3'
                                            t-att-title="attachment.name" t-att-data-mimetype="attachment.mimetype"
                                            t-attf-data-src="/web/image/#{attachment.id}/50x40?access_token=#{attachment.access_token}">
                                        </div>
                                        <div class='oe_attachment_name text-truncate'><t t-esc='attachment.name'/></div>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </t>
                   </div>
               </div>
        </t>
    </template>
    <template id="portal_my_home_documents_portal_user" name="Documentos de Contacto" customize_show="True"  inherit_id="portal.portal_my_home" priority="200">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_client_category_enable" t-value="True"/>
            <t t-set="portal_alert_category_enable" t-value="True"/>
        </xpath>
        <div id="portal_service_category" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/documents/static/description/icon.svg'"/>
                <t t-set="title">Documentos Usuario</t>
                <t t-set="url" t-value="'/odoo/user_documents'"/>
                <t t-set="text">Accede a tus documentos de usuario</t>
                <t t-set="placeholder_count" t-value="'document_count'"/>
                <t t-set="config_card" t-value="True"/>
            </t>
        </div>
    </template>
</odoo>
